<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pen Pencil Ruler - Studying made easier</title>
  <link rel="shortcut icon" href="<%= image_path('pencil.png') %>">
  <link href='//fonts.googleapis.com/css?family=Lato' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    "application-main", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  
<% [:notice, :error, :alert].each do |level| %>
  <% unless flash[level].blank? %>
    <div class="main">
  <div class="maxwidth"><div class="alert-message">
      <%= content_tag :p, flash[level] %>
      </div></div>
    </div>
  <% end %>
<% end %>

  <header>
    <div class="maxwidth">
      <span class="userinfo"><% if user_signed_in? %><input type="checkbox" id="usertoggle"><span class="expand">
            <label for="usertoggle"><%= current_user.name %></label>
            <%= link_to "Edit account", edit_user_registration_path %>
            <%= link_to "Sign out", logout_path %>
          </span>
        <% else %>
          <a href="signup" class="signup">Sign up</a>
          <a href="login" class="login">Login</a>
        <% end %>
      </span>
      <a href="/" class="home"><img src="<%= image_path("pencil.png") %>" class="navlogo"></a>
      <span class="navcurrent"><%= @title %></span>
    </div>
  </header>
  <div class="main">
  <div class="maxwidth">
<%= yield %>
</div>
</div>
<footer>
  <div class="maxwidth">
    <a href="http://www.jackhughesweb.com" class="hint--top hint--rounded" data-hint="Created by Jack Hughes (@jackhughesweb)"><img src="<%= image_path('jh.png') %>" width="25" height="25"></a>
    <br>
    <%= link_to "Terms of Service", terms_url %>
    -
    <%= link_to "Privacy Policy", privacy_url %>
    -
    <%= link_to "Feedback", feedback_url %>
    <br>
  </div>
</footer>
</body>
</html>
