<%= render 'form' %>

<br>
<%= link_to "Back to Notes", subject_topic_notes_path(@subject, @topic), class: "button" %>
<br>
<br>
<%= link_to "Delete Note", [@subject, @topic, @note], method: :delete, data: { confirm: 'Are you sure?' }, class: "button" %>
