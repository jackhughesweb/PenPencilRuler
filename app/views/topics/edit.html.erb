<%= render 'form' %>

<br>
<%= link_to "Back to Topics", subject_topics_path(@subject), class: "button" %>
<br>
<br>
<%= link_to "Delete Topic", [@subject, @topic], method: :delete, data: { confirm: 'Are you sure?' }, class: "button" %>